# removing different elements from Vector and Map
*After containers are created, a random number of items are removed. After that, different elements are removed
usage : ``` <progname> <containersSize> ```
